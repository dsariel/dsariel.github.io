<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://dsariel.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://dsariel.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-17T15:09:58+00:00</updated><id>https://dsariel.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Best Practices for Claude Code</title><link href="https://dsariel.github.io/blog/2026/Best-Practices-for-Claude-Code/" rel="alternate" type="text/html" title="Best Practices for Claude Code"/><published>2026-04-10T09:50:00+00:00</published><updated>2026-04-10T09:50:00+00:00</updated><id>https://dsariel.github.io/blog/2026/Best-Practices-for-Claude-Code</id><content type="html" xml:base="https://dsariel.github.io/blog/2026/Best-Practices-for-Claude-Code/"><![CDATA[<h1 id="best-practices-for-claude-code">Best Practices for Claude Code</h1> <h2 id="table-of-contents">Table of Contents</h2> <ol> <li><a href="#architecture-layers-overview">Architecture Layers Overview</a></li> <li><a href="#system-balance-and-layering">System Balance and Layering</a></li> <li><a href="#concept-boundaries">Concept Boundaries</a></li> <li><a href="#writing-an-effective-claudemd">Writing an Effective CLAUDE.md</a></li> <li><a href="#recommended-context-layering">Recommended Context Layering</a></li> <li><a href="#best-practices-for-context-management">Best Practices for Context Management</a></li> <li><a href="#connecting-mcp-servers">Connecting MCP Servers</a></li> <li><a href="#setting-up-hooks">Setting Up Hooks</a></li> <li><a href="#designing-skills">Designing Skills</a></li> <li><a href="#creating-custom-subagents">Creating Custom Subagents</a></li> <li><a href="#installing-plugins">Installing Plugins</a></li> <li><a href="#key-takeaways">Key Takeaways</a></li> </ol> <hr/> <h2 id="architecture-layers-overview">Architecture Layers Overview</h2> <h3 id="the-six-layer-framework">The Six-Layer Framework</h3> <table> <thead> <tr> <th>Layer</th> <th>Responsibility</th> </tr> </thead> <tbody> <tr> <td><strong>CLAUDE.md / rules / memory</strong></td> <td>Long-term context: what Claude “knows”</td> </tr> <tr> <td><strong>Tools / MCP</strong></td> <td>Capabilities of action: what Claude “can do”</td> </tr> <tr> <td><strong>Skills</strong></td> <td>Methods on request: how Claude “should act”</td> </tr> <tr> <td><strong>Hooks</strong></td> <td>Obligatory behavior, without Claude’s judgment</td> </tr> <tr> <td><strong>Subagents</strong></td> <td>Isolated workers, managed autonomy</td> </tr> <tr> <td><strong>Verifiers</strong></td> <td>Closed loop: we check, we respond, we audit</td> </tr> </tbody> </table> <hr/> <h2 id="system-balance-and-layering">System Balance and Layering</h2> <h3 id="the-problem-with-unbalanced-layers">The Problem with Unbalanced Layers</h3> <p>If you strengthen only one layer, the system becomes unbalanced. CLAUDE.md is too long – the context pollutes itself; too many tools – choice becomes unclear; subagents launched everywhere – state begins to drift; verification skipped – when an error occurs, it’s completely unclear where it broke.</p> <hr/> <h2 id="concept-boundaries">Concept Boundaries</h2> <h3 id="mcp--plugin--tools--skills--hooks--subagents">MCP / Plugin / Tools / Skills / Hooks / Subagents</h3> <table> <thead> <tr> <th>Concept</th> <th>Role in Framework</th> <th>What It Solves</th> <th>Typical Misuse</th> </tr> </thead> <tbody> <tr> <td><strong>CLAUDE.md</strong></td> <td>Persistent project contract</td> <td>Commands, boundaries, prohibitions for each session</td> <td>Turn into command wiki</td> </tr> <tr> <td><strong>.claude/rules/</strong></td> <td>Path/language rules</td> <td>Directories, file types, local norms</td> <td>All rules fall into root CLAUDE.md</td> </tr> <tr> <td><strong>Built-in Tools</strong></td> <td>Built-in capabilities</td> <td>Files, commands, search</td> <td>All integrations drain to shell</td> </tr> <tr> <td><strong>MCP</strong></td> <td>External access protocol</td> <td>GitHub, Sentry, Database</td> <td>Too many servers, context overloaded</td> </tr> <tr> <td><strong>Plugin</strong></td> <td>Packet distribution</td> <td>Skills/Hooks/MCP together</td> <td>Plugin as runtime-primitive</td> </tr> <tr> <td><strong>Skill</strong></td> <td>Knowledge/workflow by request</td> <td>Gives Claude a “method of work”</td> <td>Skill as encyclopedia + deployed script</td> </tr> <tr> <td><strong>Hook</strong></td> <td>Rule override level</td> <td>Check before/after events</td> <td>Hook instead of all model decisions</td> </tr> <tr> <td><strong>Subagent</strong></td> <td>Isolated unit of work</td> <td>Parallel research, scoped instruments</td> <td>Unbounded fan-out, chaos management</td> </tr> </tbody> </table> <h3 id="key-distinctions">Key Distinctions</h3> <ul> <li><strong>New capabilities for Claude</strong> – Tool/MCP</li> <li><strong>A set of working methods</strong> – Skill</li> <li><strong>Isolated execution environment</strong> – Subagent</li> <li><strong>Forced constraints and audit</strong> – Hook</li> <li><strong>Distribution across projects</strong> – Plugin</li> </ul> <hr/> <h2 id="writing-an-effective-claudemd">Writing an Effective CLAUDE.md</h2> <h3 id="overview">Overview</h3> <p>Run <code class="language-plaintext highlighter-rouge">/init</code> to generate a starter CLAUDE.md file based on your current project structure, then refine over time. CLAUDE.md is a special file that Claude reads at the start of every conversation. Include Bash commands, code style, and workflow rules. This gives Claude persistent context it can’t infer from code alone.</p> <h3 id="keep-it-short-and-focused">Keep It Short and Focused</h3> <p>Keep it concise. For each line, ask: “Would removing this cause Claude to make mistakes?” If not, cut it. Bloated CLAUDE.md files cause Claude to ignore your actual instructions!</p> <p><strong>Target size:</strong> Anthropic’s official CLAUDE.md is approximately 2.5K tokens—use that as a reference point.</p> <h3 id="what-to-include-vs-exclude">What to Include vs. Exclude</h3> <table> <thead> <tr> <th>✅ Include</th> <th>❌ Exclude</th> </tr> </thead> <tbody> <tr> <td>Bash commands Claude can’t guess</td> <td>Anything Claude can figure out by reading code</td> </tr> <tr> <td>Code style rules that differ from defaults</td> <td>Standard language conventions Claude already knows</td> </tr> <tr> <td>Testing instructions and preferred test runners</td> <td>Detailed API documentation (link to docs instead)</td> </tr> <tr> <td>Repository etiquette (branch naming, PR conventions)</td> <td>Information that changes frequently</td> </tr> <tr> <td>Architectural decisions specific to your project</td> <td>Long explanations or tutorials</td> </tr> <tr> <td>Developer environment quirks (required env vars)</td> <td>File-by-file descriptions of the codebase</td> </tr> <tr> <td>Common gotchas or non-obvious behaviors</td> <td>Self-evident practices like “write clean code”</td> </tr> </tbody> </table> <h3 id="example-claudemd">Example CLAUDE.md</h3> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># Code style</span>
<span class="p">-</span> Use ES modules (import/export) syntax, not CommonJS (require)
<span class="p">-</span> Destructure imports when possible (eg. import { foo } from 'bar')

<span class="gh"># Workflow</span>
<span class="p">-</span> Be sure to typecheck when you're done making a series of code changes
<span class="p">-</span> Prefer running single tests, and not the whole test suite, for performance
</code></pre></div></div> <h3 id="file-locations">File Locations</h3> <p>You can place CLAUDE.md files in several locations:</p> <ul> <li><strong>Home folder (<code class="language-plaintext highlighter-rouge">~/.claude/CLAUDE.md</code>)</strong>: applies to all Claude sessions</li> <li><strong>Project root (<code class="language-plaintext highlighter-rouge">./CLAUDE.md</code>)</strong>: check into git to share with your team</li> <li><strong>Project root (<code class="language-plaintext highlighter-rouge">./CLAUDE.local.md</code>)</strong>: personal project-specific notes; add this file to your <code class="language-plaintext highlighter-rouge">.gitignore</code> so it isn’t shared with your team</li> <li><strong>Parent directories</strong>: useful for monorepos where both <code class="language-plaintext highlighter-rouge">root/CLAUDE.md</code> and <code class="language-plaintext highlighter-rouge">root/foo/CLAUDE.md</code> are pulled in automatically</li> <li><strong>Child directories</strong>: Claude pulls in child CLAUDE.md files on demand when working with files in those directories</li> </ul> <h3 id="importing-additional-files">Importing Additional Files</h3> <p>CLAUDE.md files can import additional files using <code class="language-plaintext highlighter-rouge">@path/to/import</code> syntax:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>See @README.md for project overview and @package.json for available npm commands.

<span class="gh"># Additional Instructions</span>
<span class="p">-</span> Git workflow: @docs/git-instructions.md
<span class="p">-</span> Personal overrides: @~/.claude/my-project-instructions.md
</code></pre></div></div> <hr/> <h2 id="recommended-context-layering">Recommended Context Layering</h2> <h3 id="the-layering-strategy">The Layering Strategy</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Always resident      → CLAUDE.md: project contract / build team / prohibitions
On load path         → rules: rules for languages / directories / file types
On demand            → Skills: workflows / domain knowledge
Isolated loading     → Subagents: mass research / parallel investigations
Outside context      → Hooks: deterministic scripts / audit / blocking
</code></pre></div></div> <h3 id="core-principle">Core Principle</h3> <p>In other words, what is used rarely should not be loaded every time.</p> <hr/> <h2 id="best-practices-for-context-management">Best Practices for Context Management</h2> <h3 id="essential-guidelines">Essential Guidelines</h3> <ol> <li> <p><strong>Keep CLAUDE.md short, strict, and executable</strong> – Prioritize commands, constraints, architectural boundaries</p> </li> <li> <p><strong>Move large reference documents to supporting files for Skills</strong> – Don’t stuff them into the body of SKILL.md</p> </li> <li> <p><strong>Use <code class="language-plaintext highlighter-rouge">.claude/rules/</code> for path/language rules</strong> – Don’t overload the root CLAUDE.md</p> </li> <li> <p><strong>In long sessions, actively monitor context usage</strong> – Use <code class="language-plaintext highlighter-rouge">/context</code> command to track token consumption</p> </li> </ol> <h3 id="context-practices">Context Practices</h3> <ul> <li><strong>When switching tasks:</strong> Use <code class="language-plaintext highlighter-rouge">/clear</code> to reset context between unrelated work</li> <li><strong>When moving to a new stage of the same task:</strong> Use <code class="language-plaintext highlighter-rouge">/compact</code> to condense history while preserving key information</li> <li><strong>Write Compact Instructions in CLAUDE.md</strong> – Control what survives compression, not the algorithm</li> </ul> <h3 id="managing-context-aggressively">Managing Context Aggressively</h3> <p>During long sessions, Claude’s context window can fill with irrelevant conversation, file contents, and commands. This can reduce performance and sometimes distract Claude.</p> <ul> <li>Use <code class="language-plaintext highlighter-rouge">/clear</code> frequently between tasks to reset the context window entirely</li> <li>When auto compaction triggers, Claude summarizes what matters most, including code patterns, file states, and key decisions</li> <li>For more control, run <code class="language-plaintext highlighter-rouge">/compact &lt;instructions&gt;</code>, like <code class="language-plaintext highlighter-rouge">/compact Focus on the API changes</code></li> <li>To compact only part of the conversation, use <code class="language-plaintext highlighter-rouge">Esc + Esc</code> or <code class="language-plaintext highlighter-rouge">/rewind</code>, select a message checkpoint, and choose <strong>Summarize from here</strong></li> <li>Customize compaction behavior in CLAUDE.md with instructions like <code class="language-plaintext highlighter-rouge">"When compacting, always preserve the full list of modified files and any test commands"</code> to ensure critical context survives summarization</li> </ul> <hr/> <h2 id="connecting-mcp-servers">Connecting MCP Servers</h2> <h3 id="what-are-mcp-servers">What Are MCP Servers?</h3> <p>With MCP servers, you can ask Claude to implement features from issue trackers, query databases, analyze monitoring data, integrate designs from Figma, and automate workflows.</p> <h3 id="how-to-connect">How to Connect</h3> <p>Run <code class="language-plaintext highlighter-rouge">claude mcp add</code> to connect external tools like Notion, Figma, or your database.</p> <p>This enables Claude to interact with external services like:</p> <ul> <li>GitHub</li> <li>Sentry</li> <li>Databases</li> <li>Figma designs</li> <li>Issue trackers</li> <li>Monitoring systems</li> </ul> <h3 id="context-efficiency">Context Efficiency</h3> <p>CLI tools are the most context-efficient way to interact with external services. Use them whenever possible.</p> <hr/> <h2 id="setting-up-hooks">Setting Up Hooks</h2> <h3 id="what-are-hooks">What Are Hooks?</h3> <p>Hooks run scripts automatically at specific points in Claude’s workflow. Unlike CLAUDE.md instructions which are advisory, hooks are deterministic and guarantee the action happens.</p> <h3 id="when-to-use-hooks">When to Use Hooks</h3> <p>Use hooks for actions that <strong>must happen every time with zero exceptions</strong>. They’re appropriate for:</p> <ul> <li>Automated validation (e.g., linting)</li> <li>Blocking certain operations (e.g., preventing writes to migrations folder)</li> <li>Required side-effect actions that cannot be missed</li> </ul> <h3 id="example-hook-scenarios">Example Hook Scenarios</h3> <p>Claude can write hooks for you. Try prompts like <em>“Write a hook that runs eslint after every file edit”</em> or <em>“Write a hook that blocks writes to the migrations folder.”</em> Edit <code class="language-plaintext highlighter-rouge">.claude/settings.json</code> directly to configure hooks by hand, and run <code class="language-plaintext highlighter-rouge">/hooks</code> to browse what’s configured.</p> <h3 id="hooks-vs-claudemd-instructions">Hooks vs. CLAUDE.md Instructions</h3> <ul> <li><strong>Hooks</strong> = Mandatory, deterministic execution</li> <li><strong>CLAUDE.md</strong> = Advisory guidelines that Claude may overlook if context is crowded</li> </ul> <hr/> <h2 id="designing-skills">Designing Skills</h2> <h3 id="official-definition">Official Definition</h3> <p>A <strong>Skill</strong> is “knowledge and workflows loaded on request.” The descriptor stays in context permanently, while full content loads on demand – this is very different from a “saved prompt.”</p> <h3 id="why-skills-matter">Why Skills Matter</h3> <p>CLAUDE.md is loaded every session, so only include things that apply broadly. For domain knowledge or workflows that are only relevant sometimes, use skills instead. Claude loads them on demand without bloating every conversation.</p> <h3 id="what-a-good-skill-should-meet">What a Good Skill Should Meet</h3> <ol> <li><strong>Description tells the model “when to use me,” not “what I do”</strong> – The difference is huge</li> <li><strong>Complete steps, inputs, outputs, and stopping conditions</strong> – Not just a beginning without an end</li> <li><strong>Main text only – navigation and key constraints</strong> – Move voluminous materials to supporting files</li> <li><strong>Side effects require explicit safeguards</strong> – Use <code class="language-plaintext highlighter-rouge">disable-model-invocation: true</code> for workflows you want to trigger manually</li> </ol> <h3 id="skill-structure">Skill Structure</h3> <p>Create a skill by adding a directory with a <code class="language-plaintext highlighter-rouge">SKILL.md</code> to <code class="language-plaintext highlighter-rouge">.claude/skills/</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.claude/skills/
└── incident-triage/
    ├── SKILL.md
    ├── runbook.md
    ├── examples.md
    └── scripts/
        └── collect-context.sh
</code></pre></div></div> <h3 id="progressive-disclosure-pattern">Progressive Disclosure Pattern</h3> <p>The Claude Code team repeatedly emphasized the concept of “progressive disclosure” in their internal design – the idea is not for the model to see all information at once, but to first get an index and navigation, then pull in details as needed:</p> <ul> <li>SKILL.md defines task semantics, boundaries, and execution skeleton</li> <li>Supporting files provide domain details</li> <li>Scripts deterministically collect context or evidence</li> </ul> <h3 id="creating-skills">Creating Skills</h3> <p>Skills extend Claude’s knowledge with information specific to your project, team, or domain. Claude applies them automatically when relevant, or you can invoke them directly with <code class="language-plaintext highlighter-rouge">/skill-name</code>.</p> <p>Example skill definition:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">api-conventions</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">REST API design conventions for our services</span>
<span class="nn">---</span>
<span class="c1"># API Conventions</span>
<span class="pi">-</span> <span class="s">Use kebab-case for URL paths</span>
<span class="pi">-</span> <span class="s">Use camelCase for JSON properties</span>
<span class="pi">-</span> <span class="s">Always include pagination for list endpoints</span>
<span class="pi">-</span> <span class="s">Version APIs in the URL path (/v1/, /v2/)</span>
</code></pre></div></div> <h3 id="three-typical-skill-types">Three Typical Skill Types</h3> <h4 id="type-1-checklist-quality-barrier">Type 1: Checklist (Quality Barrier)</h4> <p>A pre-release run to make sure nothing is missed:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">release-check</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">Use before cutting a release to verify build, version, and smoke test.</span>
<span class="nn">---</span>

<span class="c1">## Pre-flight (All must pass)</span>
<span class="pi">-</span> <span class="err">`</span><span class="s">cargo build --release` passes</span>
<span class="pi">-</span> <span class="err">`</span><span class="s">cargo clippy -D warnings` clean</span>
<span class="pi">-</span> <span class="s">Version bumped in Cargo.toml</span>
<span class="pi">-</span> <span class="s">CHANGELOG updated</span>
<span class="pi">-</span> <span class="err">`</span><span class="s">kaku doctor` passes on clean env</span>

<span class="c1">## Output</span>
<span class="s">Pass / Fail per item. Any Fail must be fixed before release.</span>
</code></pre></div></div> <h4 id="type-2-workflow-standardized-operation">Type 2: Workflow (Standardized Operation)</h4> <p>Config migration – high risk, explicit invocation + built-in rollback steps:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">config-migration</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">Migrate config schema. Run only when explicitly requested.</span>
<span class="na">disable-model-invocation</span><span class="pi">:</span> <span class="kc">true</span>
<span class="nn">---</span>

<span class="c1">## Steps</span>
<span class="na">1. Backup</span><span class="pi">:</span> <span class="err">`</span><span class="s">cp ~/.config/kaku/config.toml ~/.config/kaku/config.toml.bak`</span>
<span class="na">2. Dry run</span><span class="pi">:</span> <span class="err">`</span><span class="s">kaku config migrate --dry-run`</span>
<span class="na">3. Apply</span><span class="pi">:</span> <span class="s">remove `--dry-run` after confirming output</span>
<span class="na">4. Verify</span><span class="pi">:</span> <span class="err">`</span><span class="s">kaku doctor` all pass</span>

<span class="c1">## Rollback</span>
<span class="err">`</span><span class="s">cp ~/.config/kaku/config.toml.bak ~/.config/kaku/config.toml`</span>
</code></pre></div></div> <h4 id="type-3-domain-expert-encapsulated-decision-making">Type 3: Domain Expert (Encapsulated Decision-Making)</h4> <p>When there are runtime issues, Claude collects evidence along a fixed path:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">runtime-diagnosis</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">Use when kaku crashes, hangs, or behaves unexpectedly at runtime.</span>
<span class="nn">---</span>

<span class="c1">## Evidence Collection</span>
<span class="s">1. Run `kaku doctor` and capture full output</span>
<span class="s">2. Last 50 lines of `~/.local/share/kaku/logs/`</span>
<span class="na">3. Plugin state</span><span class="pi">:</span> <span class="err">`</span><span class="s">kaku --list-plugins`</span>

<span class="c1">## Decision Matrix</span>
<span class="pi">|</span> <span class="err">Symptom</span>             <span class="err">|</span> <span class="err">First</span> <span class="err">Check</span>                         <span class="err">|</span>
<span class="err">|</span><span class="s">---------------------|-------------------------------------|</span>
<span class="err">|</span><span class="s"> Crash on startup    | doctor output → Lua syntax error    |</span>
<span class="err">|</span><span class="s"> Rendering glitch    | GPU backend / terminal capability   |</span>
<span class="err">|</span><span class="s"> Config not applied  | Config path + schema version        |</span>

<span class="err">#</span><span class="s"># Output Format</span>
<span class="err">R</span><span class="s">oot cause / Blast radius / Fix steps / Verification command</span>
</code></pre></div></div> <h3 id="descriptor-optimization">Descriptor Optimization</h3> <p>Keep descriptors short – each Skill eats up context space.</p> <p><strong>Inefficient (~45 tokens):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>description: &gt;
  This skill helps you review code changes in Rust projects.
  It checks for common issues like unsafe code, error handling...
  Use this when you want to ensure code quality before merging.
</code></pre></div></div> <p><strong>Efficient (~9 tokens):</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>description: Use for PR reviews with focus on correctness.
</code></pre></div></div> <h3 id="auto-invocation-strategy">Auto-Invocation Strategy</h3> <table> <thead> <tr> <th>Frequency</th> <th>Strategy</th> <th>Action</th> </tr> </thead> <tbody> <tr> <td>High (&gt;1 per session)</td> <td>Keep auto-invoke</td> <td>Optimize descriptor</td> </tr> <tr> <td>Low (&lt;1 per session)</td> <td>Disable auto-invoke</td> <td>Manual invocation, exclude from context</td> </tr> <tr> <td>Very low (&lt;1 per month)</td> <td>Delete Skill</td> <td>Move to AGENTS.md documentation</td> </tr> </tbody> </table> <h3 id="skills-antipatterns-to-avoid">Skills Antipatterns to Avoid</h3> <ul> <li><strong>Description too short:</strong> <code class="language-plaintext highlighter-rouge">description: help with backend</code> (triggers on any backend work)</li> <li><strong>Main text too long:</strong> Hundreds of lines crammed into SKILL.md</li> <li><strong>One Skill, too many tasks:</strong> Review + deploy + debug + docs + incident = chaos</li> <li><strong>Side effects without safeguards:</strong> Allows automatic invocation by the model</li> </ul> <hr/> <h2 id="creating-custom-subagents">Creating Custom Subagents</h2> <h3 id="what-are-subagents">What Are Subagents?</h3> <p>Subagents run in their own context with their own set of allowed tools. They’re useful for tasks that read many files or need specialized focus without cluttering your main conversation.</p> <h3 id="why-use-subagents">Why Use Subagents?</h3> <p>Since context is your fundamental constraint, subagents are one of the most powerful tools available. When Claude researches a codebase it reads lots of files, all of which consume your context. Subagents run in separate context windows and report back summaries.</p> <h3 id="creating-a-subagent">Creating a Subagent</h3> <p>Define specialized assistants in <code class="language-plaintext highlighter-rouge">.claude/agents/</code> that Claude can delegate to for isolated tasks.</p> <p>Example:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">security-reviewer</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">Reviews code for security vulnerabilities</span>
<span class="na">tools</span><span class="pi">:</span> <span class="s">Read, Grep, Glob, Bash</span>
<span class="na">model</span><span class="pi">:</span> <span class="s">opus</span>
<span class="nn">---</span>
<span class="na">You are a senior security engineer. Review code for</span><span class="pi">:</span>
<span class="pi">-</span> <span class="s">Injection vulnerabilities (SQL, XSS, command injection)</span>
<span class="pi">-</span> <span class="s">Authentication and authorization flaws</span>
<span class="pi">-</span> <span class="s">Secrets or credentials in code</span>
<span class="pi">-</span> <span class="s">Insecure data handling</span>

<span class="s">Provide specific line references and suggested fixes.</span>
</code></pre></div></div> <h3 id="invoking-subagents">Invoking Subagents</h3> <p>Tell Claude to use subagents explicitly: <em>“Use a subagent to review this code for security issues.”</em></p> <h3 id="investigation-pattern">Investigation Pattern</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Use subagents to investigate how our authentication system handles token
refresh, and whether we have any existing OAuth utilities I should reuse.
</code></pre></div></div> <p>The subagent explores the codebase, reads relevant files, and reports back with findings, all without cluttering your main conversation.</p> <hr/> <h2 id="installing-plugins">Installing Plugins</h2> <h3 id="what-are-plugins">What Are Plugins?</h3> <p>Plugins bundle skills, hooks, subagents, and MCP servers into a single installable unit from the community and Anthropic. If you work with a typed language, install a code intelligence plugin to give Claude precise symbol navigation and automatic error detection after edits.</p> <h3 id="how-to-install">How to Install</h3> <p>Run <code class="language-plaintext highlighter-rouge">/plugin</code> to browse the marketplace. Plugins add skills, tools, and integrations without configuration.</p> <h3 id="choosing-between-features">Choosing Between Features</h3> <p>For guidance on choosing between skills, subagents, hooks, and MCP, see the Extend Claude Code documentation.</p> <hr/> <h2 id="key-takeaways">Key Takeaways</h2> <h3 id="system-design">System Design</h3> <ol> <li><strong>Balance is critical</strong> – Strengthen all layers proportionally, not just one</li> <li><strong>Context is precious</strong> – Load what’s needed, when it’s needed</li> <li><strong>Layering strategy matters</strong> – Always resident → on load path → on demand → isolated → outside context</li> </ol> <h3 id="claudemd-guidelines">CLAUDE.md Guidelines</h3> <ol> <li><strong>Keep it short</strong> (~2.5K tokens target)</li> <li><strong>Make it executable</strong> – Commands, constraints, boundaries</li> <li><strong>Prune aggressively</strong> – If Claude can figure it out, don’t document it</li> <li><strong>Check it into git</strong> – Share with your team, it compounds in value</li> </ol> <h3 id="skills-design">Skills Design</h3> <ol> <li><strong>Skills are workflows, not documentation</strong> – They enable doing, not just knowing</li> <li><strong>Descriptors control invocation</strong> – Make them tell the story of “when to use me”</li> <li><strong>Progressive disclosure</strong> – Index first, details on demand</li> <li><strong>Side effects need safeguards</strong> – Use <code class="language-plaintext highlighter-rouge">disable-model-invocation: true</code> for risky operations</li> </ol> <h3 id="context-management">Context Management</h3> <ol> <li><strong>Use <code class="language-plaintext highlighter-rouge">/clear</code> between unrelated tasks</strong> – Reset context to prevent pollution</li> <li><strong>Use <code class="language-plaintext highlighter-rouge">/compact</code> for progress within a task</strong> – Condense history while preserving decisions</li> <li><strong>Monitor token usage</strong> – Use <code class="language-plaintext highlighter-rouge">/context</code> command actively</li> <li><strong>Delegate with subagents</strong> – Keep main conversation clean for implementation</li> </ol> <h3 id="hook-strategy">Hook Strategy</h3> <ol> <li><strong>Hooks = Deterministic execution</strong> – Use for actions that must happen every time</li> <li><strong>CLAUDE.md = Advisory</strong> – Use for guidelines Claude should follow</li> <li><strong>Think fail-safe</strong> – Hooks should block or enforce, not suggest</li> </ol> <h3 id="mcp-integration">MCP Integration</h3> <ol> <li><strong>Context-efficient</strong> – Use CLI tools and MCP servers instead of APIs</li> <li><strong>Flexible</strong> – Connect databases, issue trackers, design tools, monitoring systems</li> <li><strong>Scalable</strong> – Start with one tool, add more as needed</li> </ol> <hr/> <h2 id="summary-the-complete-picture">Summary: The Complete Picture</h2> <p>A well-designed Claude system has:</p> <ul> <li><strong>A lean CLAUDE.md</strong> (~2.5K tokens) with essential commands and constraints</li> <li><strong>Clear rules in <code class="language-plaintext highlighter-rouge">.claude/rules/</code></strong> for language/directory-specific guidance</li> <li><strong>Domain skills in <code class="language-plaintext highlighter-rouge">.claude/skills/</code></strong> loaded on demand for specialized knowledge</li> <li><strong>Deterministic hooks</strong> for actions that must happen without fail</li> <li><strong>Specialized subagents</strong> for isolated research or verification</li> <li><strong>Connected MCP servers</strong> for external tool integration</li> <li><strong>Plugins</strong> bundling related functionality together</li> </ul> <p>This architecture ensures that Claude has the information it needs, when it needs it, without context pollution or missed opportunities.</p> <hr/> <p><em>This guide synthesizes best practices from <a href="https://code.claude.com/docs/en/best-practices">Anthropic’s Claude Code</a> documentation and <a href="https://habr.com/ru/users/akimovpro/">akimovpro’s</a> <a href="https://habr.com/ru/articles/1012412/">article</a> on proven patterns for building scalable, maintainable Claude systems with sustainable context management wich is based on <a href="https://github.com/tw93">tw93’s</a> original <a href="https://x.com/HiTw93/status/2032091246588518683">article</a>.</em></p>]]></content><author><name></name></author><category term="Claude"/><category term="Code"/><category term="claude"/><category term="architecture"/><category term="skills"/><category term="context-management"/><category term="MCP"/><category term="plugins"/><category term="hooks"/><category term="subagents"/><category term="best-practices"/><category term="workflow-design"/><category term="prompt-engineering"/><category term="system-design"/><category term="tool-integration"/><category term="context-optimization"/><category term="ai-engineering"/><summary type="html"><![CDATA[Based on https://code.claude.com/docs/en/best-practices and https://habr.com/ru/articles/1012412/]]></summary></entry><entry><title type="html">Debugging OpenStack Tempest Pods in OpenShift Series: Failure to Import a 997MB qcow2 Image</title><link href="https://dsariel.github.io/blog/2025/howto-debug-tempest-pod-failure/" rel="alternate" type="text/html" title="Debugging OpenStack Tempest Pods in OpenShift Series: Failure to Import a 997MB qcow2 Image"/><published>2025-06-10T16:15:00+00:00</published><updated>2025-06-10T16:15:00+00:00</updated><id>https://dsariel.github.io/blog/2025/howto-debug-tempest-pod-failure</id><content type="html" xml:base="https://dsariel.github.io/blog/2025/howto-debug-tempest-pod-failure/"><![CDATA[<p>I was running OpenStack Tempest tests in my OpenShift cluster when I hit a problem. The pod <code class="language-plaintext highlighter-rouge">tempest-tests-tempest-workflow-step-00-multi-thread-testing</code> failed during image import. The issue was with a 997MB qcow2 image that would not import properly. Here’s how I solved this step by step.</p> <h2 id="the-problem">The Problem</h2> <p>My Tempest pod was failing during the image creation phase. The logs showed it was stuck trying to import a 997MB qcow2 image, and it kept timing out after 300 seconds. This is a common problem when working with large images.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Current status: importing. Waiting <span class="k">for </span>image to become active...
</code></pre></div></div> <p>This message was just looping forever until the pod gave up and died.</p> <h2 id="step-1-you-cannot-exec-into-a-failed-pod">Step 1: You Cannot Exec Into a Failed Pod</h2> <p>First thing - you cannot exec into a failed pod:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>oc <span class="nb">exec</span> <span class="nt">-it</span> tempest-tests-tempest-workflow-step-00-multi-thread-testing <span class="nt">--</span> /bin/bash
</code></pre></div></div> <p>And got this error message:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>error: cannot exec into a container in a completed pod; current phase is Failed
</code></pre></div></div> <p>This makes sense. Failed pods do not respond to commands.</p> <h2 id="step-2-check-the-pod-logs-and-events">Step 2: Check the Pod Logs and Events</h2> <p>Before doing anything else, I needed to understand what happened:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Get the pod logs</span>
oc logs tempest-tests-tempest-workflow-step-00-multi-thread-testing

<span class="c"># Get detailed information about the pod</span>
oc describe pod tempest-tests-tempest-workflow-step-00-multi-thread-testing

<span class="c"># Check events related to this pod</span>
oc get events <span class="nt">--field-selector</span> involvedObject.name<span class="o">=</span>tempest-tests-tempest-workflow-step-00-multi-thread-testing
</code></pre></div></div> <p>The logs showed me that the script was downloading a nearly 1GB image and then trying to import it into OpenStack Glance, but it was timing out during the import phase.</p> <h2 id="step-3-handle-openshift-security-policies">Step 3: Handle OpenShift Security Policies</h2> <p>Now I needed to create a debug pod. OpenShift has strict security policies. I tried the simple approach:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>oc run debug-tempest <span class="nt">--image</span><span class="o">=</span>tempest-tests-tempest-workflow-step-00-multi-thread-testing <span class="nt">--rm</span> <span class="nt">-it</span> <span class="nt">--</span> /bin/bash
</code></pre></div></div> <p>And got multiple security violations:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Warning: would violate PodSecurity "restricted:latest": allowPrivilegeEscalation != false...
</code></pre></div></div> <p>I needed to follow OpenShift’s security requirements.</p> <h2 id="step-4-create-a-security-compliant-debug-pod">Step 4: Create a Security-Compliant Debug Pod</h2> <p>I created a proper YAML manifest that meets OpenShift’s security policies:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">debug-tempest</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Never</span>
  <span class="na">securityContext</span><span class="pi">:</span>
    <span class="na">runAsNonRoot</span><span class="pi">:</span> <span class="kc">true</span>
    <span class="na">runAsUser</span><span class="pi">:</span> <span class="m">1001</span>
    <span class="na">runAsGroup</span><span class="pi">:</span> <span class="m">1001</span>
    <span class="na">fsGroup</span><span class="pi">:</span> <span class="m">1001</span>
    <span class="na">seccompProfile</span><span class="pi">:</span>
      <span class="na">type</span><span class="pi">:</span> <span class="s">RuntimeDefault</span>
  <span class="na">containers</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">debug-tempest</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">YOUR_ACTUAL_IMAGE_HERE</span>  <span class="c1"># Get this from the failed pod</span>
    <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">/bin/bash"</span><span class="pi">]</span>
    <span class="na">args</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">-c"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">sleep</span><span class="nv"> </span><span class="s">3600"</span><span class="pi">]</span>
    <span class="na">securityContext</span><span class="pi">:</span>
      <span class="na">allowPrivilegeEscalation</span><span class="pi">:</span> <span class="kc">false</span>
      <span class="na">capabilities</span><span class="pi">:</span>
        <span class="na">drop</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">ALL</span>
      <span class="na">runAsNonRoot</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">runAsUser</span><span class="pi">:</span> <span class="m">1001</span>
      <span class="na">runAsGroup</span><span class="pi">:</span> <span class="m">1001</span>
      <span class="na">seccompProfile</span><span class="pi">:</span>
        <span class="na">type</span><span class="pi">:</span> <span class="s">RuntimeDefault</span>
    <span class="na">env</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">HOME</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s">/tmp</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">OS_CLOUD</span>
      <span class="na">value</span><span class="pi">:</span> <span class="s">default</span>
    <span class="na">volumeMounts</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">temp-dir</span>
      <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/tmp</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">var-lib-tempest</span>
      <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/var/lib/tempest</span>
  <span class="na">volumes</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">temp-dir</span>
    <span class="na">emptyDir</span><span class="pi">:</span> <span class="pi">{}</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">var-lib-tempest</span>
    <span class="na">emptyDir</span><span class="pi">:</span> <span class="pi">{}</span>
</code></pre></div></div> <p>To get the actual image name, I ran:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>oc get job tempest-tests-tempest-workflow-step-00-multi-thread-testing <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.spec.template.spec.containers[0].image}'</span>
</code></pre></div></div> <h2 id="step-5-alternative-one-line-command">Step 5: Alternative One-Line Command</h2> <p>If you prefer not to use YAML files, here is a one-line command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>oc run debug-tempest <span class="nt">--image</span><span class="o">=</span><span class="si">$(</span>oc get job tempest-tests-tempest-workflow-step-00-multi-thread-testing <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.spec.template.spec.containers[0].image}'</span><span class="si">)</span> <span class="nt">--rm</span> <span class="nt">-it</span> <span class="nt">--restart</span><span class="o">=</span>Never <span class="nt">--overrides</span><span class="o">=</span><span class="s1">'
{
  "spec": {
    "securityContext": {
      "runAsNonRoot": true,
      "runAsUser": 1001,
      "seccompProfile": {"type": "RuntimeDefault"}
    },
    "containers": [{
      "name": "debug-tempest",
      "image": "'</span><span class="si">$(</span>oc get job tempest-tests-tempest-workflow-step-00-multi-thread-testing <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.spec.template.spec.containers[0].image}'</span><span class="si">)</span><span class="s1">'",
      "command": ["/bin/bash"],
      "securityContext": {
        "allowPrivilegeEscalation": false,
        "capabilities": {"drop": ["ALL"]},
        "runAsNonRoot": true,
        "runAsUser": 1001,
        "seccompProfile": {"type": "RuntimeDefault"}
      }
    }]
  }
}'</span> <span class="nt">--</span> /bin/bash
</code></pre></div></div> <h2 id="step-6-debug-the-actual-issue">Step 6: Debug the Actual Issue</h2> <p>Once I got into the debug pod, I could start investigating:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Test if the image URL is accessible</span>
curl <span class="nt">-I</span> http://a.b.c.d/dfg-network/custom_neutron_guest_rhel_8.4.qcow2

<span class="c"># Check OpenStack connectivity</span>
<span class="nb">.</span> openstackrc
openstack image list

<span class="c"># See if the image got partially created</span>
openstack image show 11111111-1111-1111-1111-111111111111
</code></pre></div></div> <h2 id="what-i-discovered">What I Discovered</h2> <p>In my case, I discovered that the <strong>Timeout was too short</strong>. A 997MB image needs more than 5 minutes to import on our storage backend. Check how much time it takes and add 20% spear time.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Manual image upload</span>
<span class="nb">time </span>openstack image create <span class="se">\</span>
  <span class="nt">--disk-format</span> qcow2 <span class="se">\</span>
  <span class="nt">--id</span> 11111111-1111-1111-1111-111111111111 <span class="se">\</span>
  <span class="nt">--file</span> custom_neutron_guest_rhel_8.4.qcow2 <span class="se">\</span>
  <span class="nt">--public</span> <span class="se">\</span>
  custom_neutron_guest_rhel_8.4
</code></pre></div></div> <h2 id="lessons-learned">Lessons Learned</h2> <ol> <li><strong>OpenShift security is not optional</strong> - Learn to work with it, not against it</li> <li><strong>Image imports can be slow</strong> - Plan your timeouts accordingly</li> <li><strong>oc debug is your friend</strong> - When it works with the security context</li> </ol> <h2 id="bonus-tip">Bonus Tip</h2> <p>If you’re running into similar issues regularly, consider pre-loading your test images during cluster setup rather than during test execution. Your future self will thank you when tests aren’t timing out at 2 AM.</p> <p>Remember, debugging in OpenShift is like debugging anywhere else, except with more YAML and more security warnings. But hey, at least the error messages are usually pretty clear about what’s wrong!</p> <p>Happy debugging, and may your pods always run to completion!</p>]]></content><author><name></name></author><category term="OpenShift"/><category term="OpenShift"/><category term="tempest pod"/><summary type="html"><![CDATA[The issue was with a 997MB qcow2 image that would not import properly. Here's how I solved this step by step.]]></summary></entry><entry><title type="html">How to Obtain a New OpenShift Token and relogin</title><link href="https://dsariel.github.io/blog/2025/How-to-Obtain-a-New-OpenShift-Token-and-relogin/" rel="alternate" type="text/html" title="How to Obtain a New OpenShift Token and relogin"/><published>2025-02-14T09:15:16+00:00</published><updated>2025-02-14T09:15:16+00:00</updated><id>https://dsariel.github.io/blog/2025/How-to-Obtain-a-New-OpenShift-Token-and-relogin</id><content type="html" xml:base="https://dsariel.github.io/blog/2025/How-to-Obtain-a-New-OpenShift-Token-and-relogin/"><![CDATA[<h1 id="general">General</h1> <p>OpenShift login tokens expire because they are, by default, valid for a limited time (e.g., 24 hours). There are several ways to re-login.</p> <h2 id="obtain-a-token-from-a-web-console">Obtain a token from a web console</h2> <p>If you have access to the web console and re-login from there:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>oc <span class="nb">whoami</span> <span class="nt">--show-console</span>
</code></pre></div></div> <h2 id="run-a-re-login-script">Run a re-login script</h2> <p>If connecting via the web console is not an option, ssh into the controller machine and run the <a href="https://github.com/dsariel/scripts/blob/master/OpenShift/relogin.sh">relogin.sh script</a>:</p> <script>fetch("https://raw.githubusercontent.com/dsariel/scripts/master/OpenShift/relogin.sh").then(t=>t.text()).then(t=>{document.getElementById("script-content").textContent=t});</script> <pre id="script-content">Loading...</pre> <h2 id="steps-to-obtain-a-new-token-using-command-line-browser">Steps to Obtain a New Token using command line browser</h2> <p>Another (more laborious) way is using a text-based web browser on the remote server (again, if the web console is not an option). Follow these steps to obtain a new token and continue working with the cluster:</p> <ol> <li> <p><strong>Install a Text-Based Browser</strong><br/> Install <code class="language-plaintext highlighter-rouge">lynx</code>, a command-line browser, to access the token request page:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>yum <span class="nb">install </span>lynx <span class="nt">-y</span>
</code></pre></div> </div> </li> <li> <p><strong>Retrieve a password for the <code class="language-plaintext highlighter-rouge">kubeadmin</code> user</strong><br/> If you are using the default <code class="language-plaintext highlighter-rouge">kubeadmin</code> user, retrieve the password from the installation directory:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> ~/.kube/kubeadmin-password
</code></pre></div> </div> <p>Use this password to log in as <code class="language-plaintext highlighter-rouge">kubeadmin</code> in the browser interface during the next stage.</p> </li> <li> <p><strong>Access the Token Request Page</strong><br/> Use <code class="language-plaintext highlighter-rouge">lynx</code> to navigate to the token request page:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lynx https://oauth-openshift.apps.ocp.openstack.lab/oauth/token/request
</code></pre></div> </div> </li> <li> <p><strong>Obtain a New Token</strong><br/> Once logged in, obtain a new token. The login command will look like this:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>oc login <span class="nt">--token</span><span class="o">=</span>sha256~&lt;...&gt; <span class="nt">--server</span><span class="o">=</span>https://api.ocp.openstack.lab:6443
</code></pre></div> </div> </li> </ol>]]></content><author><name></name></author><category term="OpenShift"/><category term="OpenShift"/><category term="token"/><category term="xterm"/><category term="CLI"/><category term="text-web-browser"/><category term="lynx"/><category term="web-browser"/><summary type="html"><![CDATA[An OpenShift login token expired because tokens are, by default, valid for a limited time. Follow these steps to obtain a new token and continue working with the cluster.]]></summary></entry></feed>